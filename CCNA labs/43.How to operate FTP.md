## Goal:

1. Use TFTP on R1 to retrieve the following file from SRV1:c2900-universalk9-mz.SPA.155-3.M4a.bin
2. Upgrade R1's OS and then delete the old file from flash.
3. Use FTP on R2 to retrieve the following file from SRV1:c2900-universalk9-mz.SPA.155-3.M4a.bin(FTP username: jeremy, password: ccna)
4. Upgrade R2's OS and then delete the old file from flash.
    
## Topology
<img width="1094" height="328" alt="{D6D7ECBD-69A2-4660-82B6-A8B9B3423715}" src="https://github.com/user-attachments/assets/fbb06983-8b7e-4ad4-ab52-a31a411a1c25" />



## Configuration - use TFTP on R1 to retrieve the newest file .

```
R1#show version
R1#show flash
R1#copy tftp flash:
then enter the remote host and file's name
R1(config)boot system /the newest file/
R1(config)exit
R1#write
R1#reload
```
