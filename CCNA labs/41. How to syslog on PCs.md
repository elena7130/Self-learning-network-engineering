
## Goal:

R1 username: jeremy,  PW: ccna,  enable PW: ccna

1. Connect to R1's console port using PC2:
    Shut down and re-enable the G0/0 interface
    Enable timestamps for logging messages

2. Telnet from PC1 to R1's G0/0 interface
    Enable logging to on the terminal line
    Enable logging to the VTY lines for the current session.

3. Enable logging to the syslog server SRV1 with a level of 'debugging'.


## Topology

<img width="753" height="480" alt="image" src="https://github.com/user-attachments/assets/dcdeba97-3846-431b-b4cb-55922330a469" />




## Configuration -on PC2-->terminal
Enable timestamps for logging messages

```
username:jeremy
password:
R1>en
password:
R1#conf t
R1(config)#int g0/1
R1(config)#no shutdown
R1(config)#sevice timestamps log datetime 
```

## Configuration -on PC2-->Telnet
Enable logging to on the terminal line
Enable logging to the VTY lines for the current session.

```
username:jeremy
password:
R1>en
password:
R1#conf t
R1(config)#int g0/1
R1(config)#no shutdown
R1#terminal monitor

```

## Configuration -on PC2-->Telnet
Enable logging to the syslog server SRV1 with a level of 'debugging'

```
R1(config)#logging 192.168.1.100
R1(config)#logging trap debugging 
R1(config)#int g0/1
R1(config)#shutdown
then I will find the syslog exit on the SRV1
```
